/*
 * MCPanel ABI stub — no logic, no dependencies.
 * Exists solely so the APK ships a native library pinned to one ABI
 * (see CMakeLists.txt). Nothing in the app calls this.
 */
void mcpanel_stub(void) {}